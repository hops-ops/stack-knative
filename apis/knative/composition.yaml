apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: knatives.stacks.hops.ops.com.ai
spec:
  compositeTypeRef:
    apiVersion: stacks.hops.ops.com.ai/v1alpha1
    kind: Knative
  mode: Pipeline
  pipeline:
  - functionRef:
      name: hops-ops-stack-knativerender
    step: render
  - functionRef:
      name: crossplane-contrib-function-auto-ready
    step: crossplane-contrib-function-auto-ready
